import 'package:jaspr/dom.dart';

// ============================================================================
// TERMINAL THEME COLORS (Original)
// ============================================================================

/// Professional terminal theme colors - muted and elegant
const primaryText = Color('#c9d1d9');        // Soft white for main text
const secondaryText = Color('#8b949e');      // Muted gray for labels
const dimText = Color('#6e7681');            // Dim gray for separators

const accentBlue = Color('#58a6ff');         // Soft blue for links
const accentTeal = Color('#56d4dd');         // Muted teal for highlights
const accentGold = Color('#d29922');         // Subtle gold for section titles
const accentGreen = Color('#7ee787');        // Soft green for name/status
const accentPurple = Color('#bc8cff');       // Muted purple for markers
const accentOrange = Color('#f0883e');       // Soft orange for bullets

const terminalBg = Color('#0d1117');         // GitHub dark background
const terminalBgLight = Color('#161b22');    // Slightly lighter bg
const terminalBorder = Color('#30363d');     // Subtle border color

// ============================================================================
// MODERN MINIMALIST THEME COLORS
// ============================================================================

/// Background Colors
const modernBgPrimary = Color('#0F172A');    // Deep slate - main background
const modernBgSecondary = Color('#1E293B');  // Lighter slate - cards/sections
const modernBgTertiary = Color('#334155');   // Medium slate - hover states

/// Accent Colors
const modernAccentPrimary = Color('#6366F1'); // Indigo - primary accent
const modernAccentSecondary = Color('#8B5CF6'); // Violet - secondary accent
const modernAccentGradient1 = Color('#6366F1'); // Gradient start
const modernAccentGradient2 = Color('#8B5CF6'); // Gradient end

/// Text Colors
const modernTextPrimary = Color('#F1F5F9');   // Off-white - headings, main text
const modernTextSecondary = Color('#94A3B8'); // Slate gray - descriptions
const modernTextMuted = Color('#64748B');     // Dimmed slate - secondary info

/// UI Colors
const modernBorder = Color('#334155');         // Slate - borders, dividers
const modernSuccess = Color('#10B981');        // Emerald - status indicators
const modernDivider = Color('#1E293B');        // Subtle dividers

/// Shadow Colors
const modernShadow = Color('rgba(0, 0, 0, 0.3)'); // Card shadows
const modernShadowHover = Color('rgba(99, 102, 241, 0.15)'); // Hover glow
